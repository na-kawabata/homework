using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Jsonをよみとって対応するクラスに
/// </summary>
/*public class JsonReader : MonoBehaviour
{
    public void CardMasterReader()
    {
        //Jsonファイル読み込み
      　string json = Resources.Load<TextAsset>("Data/card").ToString();
        CardMaster[] cardmasterList = null;

        //取得テスト
        for(int i = 0; i<cardmasterList.Length; i++)
        {
            Debug.Log(cardmasterList[i].id);
            Debug.Log(cardmasterList[i].cardname);
            Debug.Log(cardmasterList[i].rarity);
        }

    }


}
*/
